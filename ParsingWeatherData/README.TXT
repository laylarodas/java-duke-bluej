# 🌦️ Parsing Weather Data

Practice project from the Duke Java specialization.  
Reads multiple CSV weather files and finds:
- The coldest temperature of the day.
- The file with the coldest temperature overall.
- The lowest humidity (single or multiple files).
- The average temperature (optionally filtered by humidity).

## 🧰 Tech
Java · edu.duke library · Apache Commons CSV

## ▶️ Run
Open in BlueJ → execute `testColdestHourInFile()` or `testFileWithColdestTemperature()`  
Use CSV files from the course dataset (`nc_weather`).
